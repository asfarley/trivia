[defaults]
inventory        = inventory/production.ini
remote_user      = ubuntu
private_key_file = ../.credentials/trivia.pem
host_key_checking = False

[privilege_escalation]
become        = True
become_method = sudo
